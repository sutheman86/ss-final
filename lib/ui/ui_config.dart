import 'package:flutter/material.dart';

class UIConfig extends StatefulWidget {
	const UIConfig({super.key});	
	@override
	State<UIConfig> createState() {
		return _UIConfig();
	}
}

class _UIConfig extends State<UIConfig> {
	@override
	build(context) {
		return const Scaffold();
	}
}
